import { defineConfig } from 'vite'

export default defineConfig({
  // If you are deploying to the default GitHub Pages URL (https://tgilbert826.github.io/transparent-tips-aether/):
  // Uncomment the line below.
  // base: '/transparent-tips-aether/',
  
  // If you are using a custom domain (like https://yourdomain.com):
  // Keep base set to '/' (or leave it commented out).
  base: '/',
})
